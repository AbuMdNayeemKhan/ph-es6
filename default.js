function add(num1 = 0, num2 = 0) { // use default valu for if default any value is not will set
    const result = num1 + num2;
    console.log(num1, num2, result);
    return result;
}
const sum = add(7);   